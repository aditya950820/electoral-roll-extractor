# --- web stack (FastAPI command center) ---
fastapi>=0.110              # API backend (server.py)
uvicorn[standard]>=0.27     # ASGI server that runs the app
python-multipart>=0.0.9     # multipart form parsing (PDF uploads)
itsdangerous>=2.1           # signs session cookies (Starlette SessionMiddleware)
mistralai>=1.2,<2   # 2.x reorganizes the package; app uses the 1.x client API
pymupdf>=1.24
pandas>=2.0
openpyxl>=3.1
python-dotenv>=1.0
numpy>=1.26
opencv-python-headless>=4.9   # voter-photo cropping (cv2)
psycopg[binary]>=3.1          # Postgres driver
pgvector>=0.2                 # face-embedding column support
jellyfish>=1.0                # metaphone / fuzzy name matching
